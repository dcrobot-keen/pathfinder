# node link 편집 layer 만들기

현재 2D 맵에 레이어를 추가해서 노드와 엣지를 추가할 수 있는 기능을 추가하자. 편집 내용은 GeoJSON 포맷으로 DB에 저장이 되게 만들어야 함.

DB는 파일 DB 중 GeoJSON으로 저장이 되는 가장 간단하게 git 저장소에서 받아도 되는 녀석중에 추천을 통해 결정하도록 하자 

레이어는 다음과 같아
- 노드(point)
- 링크(line)
- block(polygon)


