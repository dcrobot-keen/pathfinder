__pycache__/
*.pyc
.venv/
venv/
.claude/

# downloaded third-party sample data + generated outputs (PLY/PGM/PNG/etc.) -
# not our code, regenerate via the scripts in scripts/ instead of committing
sample_data/

# real company scan data (e.g. office.usdz) and anything derived from it --
# never commit, this is not ours to publish
data/

# scripts/studio.py project outputs -- derived from real scans, never commit
projects/
