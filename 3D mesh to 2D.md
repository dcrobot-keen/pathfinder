# 아이폰 라이다를 이용한 실내 지도 구현 
아이폰 라이다를 이용해 실내 지도를 구현하고 싶어
지도 프레임워크는 오픈레이어스를 사용하고,
컬러드 PCD 레이어 하나, 2D 레이어 하나를 올려서 뷰를 볼 수 있도록 하는 프로그램을 짜보자.

## 1. Openlayers를 이용한 지도 생성
0,0 기점으로 m 단위의 평면 좌표계 기준으로 200mX200m 기준의 실내 지도를 만들어 보자 

## 2. 3D 컬러드 PCD 지도
컬러드 PCD (3D) 데이터를 가지고 와서 화면에 표현하는 예제
샘플 PCD를 가지고와서 오픈레이어스에 올리는 샘플을 만들어 보자. 

## 3. 2D 지도를 작성
컬러드 PCD를 2D 기준의 지도로 잘라서 만들어 50cm 단위로 잘라서 레이어를 추가하자 

## 4. 3D mesh 지도
컬러드 PCD 데이터를 3D mesh 로 변경하는 모델을 이용해서 해당 pcd를 변경하는 코드를 짜보자. 이후 pcd를 다른 파일로 변경해도 활용할 수 있는 구조로 만들어줘 (명령어, 업로드 둘다)

## 5. 3D 파일 업로드시 그 파일에 맞춰서 2D 지도 자동 변환
3D 파일이 업로드가 되면 그 파일의 좌표에 맞춰 2D 지도가 자동으로 변환 되도록 인터페이스를 바꿔보자. 
파일이 바뀔때 마다 2D, 3D 같이 바뀌는 거지

## 6. PCD 파일 랜덤 생성 기능
PCD 파일 생성하는 기능을 활용하고 버튼을 누르면 만들어지고 바로 3D 지도, 2D 지도로 반영이 되는 걸 만들어 보자
